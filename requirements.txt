# ── AWS ──────────────────────────────────────────────────────
boto3>=1.34.0

# ── Azure ────────────────────────────────────────────────────
azure-identity>=1.16.0
azure-mgmt-storage>=21.0.0
azure-mgmt-network>=25.0.0
azure-mgmt-sql>=3.0.0
azure-mgmt-keyvault>=10.0.0
azure-mgmt-compute>=30.0.0

# ── GCP ──────────────────────────────────────────────────────
google-cloud-storage>=2.14.0
google-auth>=2.28.0
google-api-python-client>=2.120.0

# ── Core ─────────────────────────────────────────────────────
PyYAML>=6.0.1

# ── Testing ──────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0

# ── Notes ────────────────────────────────────────────────────
# AI advisor uses stdlib urllib only — no anthropic SDK required
# Set ANTHROPIC_API_KEY to enable AI remediation advice
# Cloud Custodian (c7n) is NOT a dependency — engine is native
